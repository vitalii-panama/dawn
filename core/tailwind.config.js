/** @type {import('tailwindcss').Config} */
export default {
    content: [
        './index.html', // Path to the index.html file
        './src/**/*.{js,ts,jsx,tsx}', // Path to all JS, TS, JSX, and TSX files in the src directory
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
